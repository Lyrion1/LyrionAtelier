{
  "name": "lyrion-atelier",
  "version": "1.0.0",
  "scripts": {
    "brand:images": "node scripts/attach-images.mjs",
    "build:images": "node scripts/build-images-map.mjs",
    "prepare:catalog": "node tools/prepare-catalog-images.mjs",
    "build": "npm run prepare:catalog && npm run build:images && npm run build:catalog && npm run test:images && npm run test:catalog",
    "optimize:images": "node scripts/optimize-images.mjs",
    "test:shop": "playwright test tests",
    "test:images": "node tests/validate-image-references.mjs",
    "fetch-printful": "node scripts/fetch-printful-variants.js",
    "build:catalog": "node scripts/build-checkout-catalog.mjs",
    "test:catalog": "node scripts/verify-catalog.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "node-fetch": "^2.7.0",
    "resend": "^3.2.0",
    "stripe": "^14.25.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "sharp": "^0.33.5"
  },
  "allowScripts": {
    "sharp@0.33.5": true
  }
}
